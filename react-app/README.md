# Dámdi QR Frontend (React + TypeScript)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка

Создайте файл `.env`:
```env
VITE_API_URL=http://localhost:3000/api
```

### 3. Запуск

**Режим разработки:**
```bash
npm run dev
```

Откроется `http://localhost:5173`

**Production сборка:**
```bash
npm run build
npm preview
```

## 📁 Структура проекта

```
react-app/
├── src/
│   ├── api/             # API клиенты
│   ├── components/      # React компоненты
│   ├── contexts/        # Context (Auth)
│   ├── pages/           # Страницы
│   ├── types/           # TypeScript типы
│   ├── App.tsx          # Главный компонент
│   └── main.tsx         # Точка входа
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

## 🎨 Технологии

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **React Router** - роутинг
- **Vite** - сборщик
- **Axios** - HTTP клиент

## 📄 Страницы

- `/login` - Страница входа
- `/dashboard/super-admin` - Панель супер-админа
- `/dashboard/admin` - Панель администратора
- `/dashboard/staff` - Панель сотрудника
- `/menu/client` - Клиентское меню (публичное)

## 🔐 Аутентификация

Токен сохраняется в `localStorage` и автоматически добавляется к запросам.

## 🎯 Следующие шаги

1. Доделать все страницы дашбордов
2. Добавить компоненты (модалки, формы)
3. Добавить обработку ошибок
4. Добавить loading states
5. Оптимизировать производительность

