# Dámdi QR - Система управления ресторанами с QR-меню

Современная SaaS-платформа для управления ресторанами, кафе и барами с цифровыми QR-меню, системой заказов и интеграцией с мессенджерами.

## 🌟 Основные возможности

- 📱 **QR-меню** — цифровые меню с автоматической генерацией QR-кодов
- 🍽️ **Система заказов** — заказы в ресторане и доставка на дом
- 📊 **Аналитика** — статистика по заказам и выручке
- 👥 **Управление персоналом** — добавление сотрудников и назначение ролей
- 📲 **WhatsApp интеграция** — автоматические уведомления о заказах доставки
- 💎 **Тарифная система** — бесплатный и Premium тарифы
- 📱 **Адаптивный дизайн** — работает на всех устройствах
- ⚡ **Real-time обновления** — мгновенное отображение новых заказов

## 🚀 Технологии

- **Backend:** Node.js + Express + TypeScript
- **Frontend:** React 18 + TypeScript + Vite
- **База данных:** MySQL 8.0+ (Docker)
- **Аутентификация:** JWT
- **QR-коды:** qrcode библиотека
- **Интеграции:** Green API / Twilio для WhatsApp

## 📦 Структура проекта

```
DAMDIQRRR/
├── nodejs/              # Backend API (Express + TypeScript)
│   ├── src/
│   │   ├── controllers/ # API контроллеры
│   │   ├── routes/      # Маршруты API
│   │   ├── middleware/  # Middleware (auth, валидация)
│   │   ├── utils/       # Утилиты (QR, тарифы, WhatsApp)
│   │   ├── config/      # Конфигурация (БД)
│   │   ├── types/       # TypeScript типы
│   │   └── server.ts    # Точка входа сервера
│   └── package.json
│
├── react-app/           # Frontend (React + TypeScript)
│   ├── src/
│   │   ├── api/         # API клиенты (Axios)
│   │   ├── pages/       # Страницы приложения
│   │   ├── components/  # Переиспользуемые компоненты
│   │   ├── contexts/    # React Context (Auth)
│   │   └── types/       # TypeScript типы
│   └── package.json
│
├── database/            # SQL схемы и миграции
│   ├── schema.sql       # Основная схема БД
│   ├── add-delivery-and-whatsapp.sql  # Миграция доставки
│   └── ...
│
├── uploads/             # Загруженные изображения
└── Example/             # Оригинальный PHP прототип (архив)
```

## 🚀 Быстрый старт

### Требования

- Node.js 18+
- MySQL 8.0+ (или Docker)
- npm или yarn

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd DAMDIQRRR
```

### 2. База данных

#### Вариант A: Docker (рекомендуется)

```bash
# Запуск MySQL в Docker
sudo ./setup-mysql-simple.sh

# Проверка подключения
docker exec damdiqr_mysql mysql -udamdi_user -pdamdi_password -e "SHOW DATABASES;"
```

#### Вариант B: Локальная MySQL

```bash
# Создать базу данных
mysql -u root -p
CREATE DATABASE damdi_qr CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;

# Импортировать схему
mysql -u root -p damdi_qr < database/schema.sql

# Применить миграции
mysql -u root -p damdi_qr < database/add-delivery-and-whatsapp.sql
```

### 3. Backend

```bash
cd nodejs

# Установка зависимостей
npm install

# Настройка окружения
cp .env.example .env
# Отредактируйте .env с настройками БД

# Запуск в режиме разработки
npm run dev
```

Backend запустится на `http://localhost:3000`

**Важные переменные в `.env`:**
```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=damdi_user
DB_PASSWORD=damdi_password
DB_NAME=damdi_qr
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=7d

# Опционально: WhatsApp API (для автоматических уведомлений)
GREEN_API_ID=your_id
GREEN_API_TOKEN=your_token
# Или
TWILIO_ACCOUNT_SID=your_sid
TWILIO_AUTH_TOKEN=your_token
TWILIO_WHATSAPP_FROM=whatsapp:+1234567890
```

### 4. Frontend

```bash
cd react-app

# Установка зависимостей
npm install

# Настройка окружения (если нужно)
# Убедитесь, что VITE_API_URL указывает на backend (по умолчанию /api)

# Запуск в режиме разработки
npm run dev
```

Frontend запустится на `http://localhost:5173`

## 🔐 Демо-аккаунты

### Супер-администратор
- **Email:** `superadmin@damdiqr.com`
- **Пароль:** `admin123`
- **Доступ:** Панель управления всеми ресторанами

### Администратор ресторана
- **Email:** `admin@demo.com`
- **Пароль:** `admin123`
- **Доступ:** Панель управления рестораном (меню, столики, сотрудники, настройки)

### Сотрудник
- **Email:** `staff@demo.com`
- **Пароль:** `staff123`
- **Доступ:** Доска заказов (Kanban)

## 📱 Основные функции

### Лендинг
- Маркетинговая страница с описанием возможностей
- Адаптивный дизайн для всех устройств
- FAQ, отзывы, тарифы

### Клиентское меню
- Просмотр меню ресторана по QR-коду
- Поиск и фильтрация блюд
- Заказ в ресторане (привязка к столику)
- **Заказ на доставку** (с указанием адреса и телефона)
- Корзина и оформление заказа

### Панель администратора
- **Главная:** статистика по заказам и выручке
- **Меню:** управление категориями и блюдами с загрузкой фото
- **Столики:** создание столиков и генерация QR-кодов
- **Сотрудники:** управление персоналом
- **Настройки:** редактирование данных ресторана и **WhatsApp номера**

### Панель сотрудника
- Kanban-доска заказов (NEW → PREPARING → READY → COMPLETED)
- Real-time обновления каждые 3 секунды
- Только заказы типа "В ресторане" (DINE_IN)

### Панель супер-администратора
- Статистика по всем ресторанам
- Создание и управление ресторанами
- Изменение тарифных планов

## 📋 API Endpoints

### Аутентификация
- `POST /api/auth/login` — вход в систему
- `GET /api/auth/me` — информация о текущем пользователе
- `POST /api/auth/logout` — выход

### Рестораны
- `POST /api/restaurants` — создать ресторан (SUPER_ADMIN)
- `GET /api/restaurants` — список ресторанов (SUPER_ADMIN)
- `GET /api/restaurants/stats` — статистика (SUPER_ADMIN)
- `GET /api/restaurants/:id/public` — публичная информация о ресторане
- `GET /api/restaurants/my` — мой ресторан (ADMIN)
- `PUT /api/restaurants/my` — обновить ресторан (ADMIN, включает WhatsApp номер)
- `GET /api/restaurants/limits` — лимиты тарифного плана (ADMIN)

### Меню
- `GET /api/menu?restaurant_id={id}` — получить меню (публичный)
- `POST /api/menu/categories` — создать категорию (ADMIN)
- `POST /api/menu/items` — создать блюдо (ADMIN)
- `PUT /api/menu/items/:id` — обновить блюдо (ADMIN)
- `DELETE /api/menu/categories/:id` — удалить категорию (ADMIN)
- `DELETE /api/menu/items/:id` — удалить блюдо (ADMIN)

### Столики
- `GET /api/tables` — список столиков (ADMIN)
- `POST /api/tables` — создать столик (ADMIN)
- `DELETE /api/tables/:id` — удалить столик (ADMIN)

### Заказы
- `POST /api/orders` — создать заказ (публичный, с поддержкой DINE_IN и DELIVERY)
- `GET /api/orders` — список заказов (ADMIN/STAFF/SUPER_ADMIN)
- `GET /api/orders/poll?last_id={id}` — получить новые заказы (STAFF, только DINE_IN)
- `PUT /api/orders/:id/status` — обновить статус заказа (STAFF)

### Сотрудники
- `GET /api/staff` — список сотрудников (ADMIN)
- `POST /api/staff` — добавить сотрудника (ADMIN)
- `DELETE /api/staff/:id` — удалить сотрудника (ADMIN)

### QR-коды
- `GET /api/qr/generate?table_id={id}` — скачать QR-код (ADMIN, PNG)
- `GET /api/qr/preview?table_id={id}` — превью QR-кода (ADMIN)

### Загрузка файлов
- `POST /api/upload/image` — загрузить изображение (ADMIN)

## 💎 Тарифная система

### FREE (Бесплатный)
- ✅ До 10 столиков
- ✅ До 5 категорий меню
- ✅ Неограниченное количество блюд
- ✅ QR-коды для столиков
- ✅ Базовая аналитика
- ✅ Email поддержка

### PREMIUM (9 990 ₸/месяц)
- ✅ До 999 столиков
- ✅ Неограниченное количество категорий
- ✅ Неограниченное количество блюд
- ✅ Расширенная аналитика
- ✅ Приоритетная поддержка 24/7
- ✅ Персональный менеджер
- ✅ Кастомизация дизайна
- ✅ API для интеграций

## 🎯 Роли пользователей

1. **SUPER_ADMIN** — управление всеми ресторанами в системе
2. **ADMIN** — владелец ресторана, управление меню, столиками, сотрудниками
3. **STAFF** — сотрудник, обработка заказов через Kanban-доску

## 📲 Интеграция с WhatsApp

### Настройка для автоматических уведомлений

Платформа поддерживает автоматическую отправку уведомлений о заказах доставки в WhatsApp.

#### Вариант 1: Green API (рекомендуется, бесплатно для тестирования)

1. Зарегистрируйтесь на https://green-api.com
2. Создайте инстанс и получите `idInstance` и `apiTokenInstance`
3. Добавьте в `nodejs/.env`:
   ```env
   GREEN_API_ID=your_id
   GREEN_API_TOKEN=your_token
   ```
4. Перезапустите backend

#### Вариант 2: Twilio

1. Зарегистрируйтесь на https://twilio.com
2. Получите `Account SID`, `Auth Token`, настройте `WhatsApp From`
3. Добавьте в `nodejs/.env`:
   ```env
   TWILIO_ACCOUNT_SID=your_sid
   TWILIO_AUTH_TOKEN=your_token
   TWILIO_WHATSAPP_FROM=whatsapp:+1234567890
   ```
4. Перезапустите backend

#### Вариант 3: Ручная отправка (без API)

Если API не настроено, после оформления заказа на доставку появляется модальное окно с кнопкой "Открыть в WhatsApp". Клиент может нажать на кнопку и отправить готовое сообщение вручную.

**Важно:** В настройках ресторана (панель администратора) необходимо указать WhatsApp номер для получения уведомлений.

Подробнее см. `HOW_TO_SETUP_WHATSAPP.md`

## 🔧 Разработка

### Запуск в режиме разработки

```bash
# Backend (hot reload)
cd nodejs
npm run dev

# Frontend (hot reload)
cd react-app
npm run dev
```

### Production build

```bash
# Backend
cd nodejs
npm run build
npm start

# Frontend (статический build)
cd react-app
npm run build
# Файлы будут в react-app/dist/
```

### Линтинг и форматирование

```bash
# Backend
cd nodejs
npm run lint

# Frontend
cd react-app
npm run lint
```

## 🗄️ База данных

### Структура

Основные таблицы:
- `users` — пользователи системы
- `restaurants` — рестораны
- `menu_categories` — категории меню
- `menu_items` — блюда
- `tables` — столики
- `orders` — заказы (с поддержкой DINE_IN и DELIVERY)
- `order_items` — позиции заказа
- `staff` — связь пользователей и ресторанов (для сотрудников)

### Миграции

Для добавления новых функций созданы SQL-миграции:
- `database/add-delivery-and-whatsapp.sql` — поля для доставки и WhatsApp

Применить миграцию:
```bash
mysql -u root -p damdi_qr < database/add-delivery-and-whatsapp.sql
```

## 📚 Документация

- **[TECHNICAL_SPECIFICATION.md](./TECHNICAL_SPECIFICATION.md)** — полное техническое задание
- **[ЗАПУСК.md](./ЗАПУСК.md)** — подробная инструкция по запуску
- **[HOW_TO_SETUP_WHATSAPP.md](./HOW_TO_SETUP_WHATSAPP.md)** — настройка WhatsApp интеграции
- **[FEATURES_ROADMAP.md](./FEATURES_ROADMAP.md)** — план развития проекта

## 🐛 Решение проблем

### Проблемы с базой данных

```bash
# Проверка подключения
docker exec damdiqr_mysql mysql -udamdi_user -pdamdi_password -e "SELECT 1;"

# Пересоздание базы данных
docker exec damdiqr_mysql mysql -udamdi_user -pdamdi_password -e "DROP DATABASE IF EXISTS damdi_qr; CREATE DATABASE damdi_qr CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
mysql -u root -p damdi_qr < database/schema.sql
```

### Проблемы с кодировкой

Убедитесь, что база данных использует UTF-8:
```sql
ALTER DATABASE damdi_qr CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### Проблемы с CORS

Убедитесь, что в `nodejs/src/server.ts` настроен CORS:
```typescript
app.use(cors({
  origin: process.env.FRONTEND_URL || 'http://localhost:5173',
  credentials: true
}));
```

## 📝 Лицензия

ISC

## 👥 Контакты

- **Email:** support@damdiqr.com
- **WhatsApp:** +7 777 123 4567
- **Сайт:** https://damdiqr.com

---

**Версия:** 1.0.0  
**Последнее обновление:** 2025-11-22
