# 🚀 КАК ЗАПУСТИТЬ ПРОЕКТ

## 📋 Быстрая инструкция (3 шага)

### ШАГ 1: Запустить MySQL (один раз)

```bash
cd /home/hubtech/Documents/DAMDIQRRR
sudo ./setup-mysql-simple.sh
```

Подождите ~15 секунд, пока MySQL запустится.

✅ Если видите "✅ MySQL готов!" — можно продолжать.

---

### ШАГ 2: Запустить Backend (терминал 1)

Откройте **новый терминал** и выполните:

```bash
cd /home/hubtech/Documents/DAMDIQRRR/nodejs
npm install  # Первый раз (если еще не установлено)
npm run dev
```

✅ Должно появиться:
```
🚀 Server running on port 3000
📡 API: http://localhost:3000/api
❤️  Health: http://localhost:3000/health
```

**Оставьте этот терминал открытым!**

---

### ШАГ 3: Запустить Frontend (терминал 2)

Откройте **еще один новый терминал** и выполните:

```bash
cd /home/hubtech/Documents/DAMDIQRRR/react-app
npm install  # Первый раз (если еще не установлено)
npm run dev
```

✅ Должно появиться:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

**Оставьте этот терминал открытым!**

---

## 🌐 Открыть в браузере

Откройте: **http://localhost:5173**

---

## 🔐 Войти в систему

### Демо-аккаунт администратора:
- **Email:** `admin@demo.com`
- **Пароль:** `admin123`

Или нажмите кнопку "🏢 Владелец заведения" на странице входа.

---

## ⚠️ Если что-то не работает

### Ошибка: "address already in use"
Порт занят. Остановите процесс:

```bash
# Остановить Backend (порт 3000)
lsof -ti:3000 | xargs kill -9

# Остановить Frontend (порт 5173)
lsof -ti:5173 | xargs kill -9
```

### Ошибка: "MySQL не подключен"
```bash
cd /home/hubtech/Documents/DAMDIQRRR
sudo ./setup-mysql-simple.sh
```

### Ошибка: "Cannot find module"
Нужно установить зависимости:

```bash
# В папке nodejs
cd /home/hubtech/Documents/DAMDIQRRR/nodejs
npm install

# В папке react-app
cd /home/hubtech/Documents/DAMDIQRRR/react-app
npm install
```

### Ошибка: ".env файл не найден"
Файл `.env` создается автоматически при запуске `setup-mysql-simple.sh`.

Если его нет, создайте вручную:

```bash
cd /home/hubtech/Documents/DAMDIQRRR/nodejs
cat > .env << 'EOF'
PORT=3000
NODE_ENV=development
DB_HOST=localhost
DB_USER=damdi_user
DB_PASSWORD=damdi_password
DB_NAME=damdi_qr
JWT_SECRET=damdiqr_super_secret_key_change_in_production_2024
JWT_EXPIRES_IN=7d
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=5242880
EOF
```

---

## 🛑 Остановить проект

В каждом терминале (Backend и Frontend) нажмите `Ctrl+C`

Остановить MySQL:
```bash
sudo docker stop damdiqr_mysql
```

Или удалить контейнер:
```bash
sudo docker stop damdiqr_mysql
sudo docker rm damdiqr_mysql
```

---

## ✅ Проверка работы

### Backend работает?
Откройте в браузере: http://localhost:3000/health

Должно быть: `{"status":"ok","timestamp":"..."}`

### Frontend работает?
Откройте в браузере: http://localhost:5173

Должна открыться главная страница (лендинг).

### База данных работает?
```bash
docker exec -it damdiqr_mysql mysql -udamdi_user -pdamdi_password damdi_qr -e "SELECT COUNT(*) as users FROM users;"
```

Должно показать количество пользователей.

---

## 🎯 Что дальше?

1. Откройте http://localhost:5173
2. Нажмите "Войти"
3. Войдите как администратор: `admin@demo.com` / `admin123`
4. Начните работу с меню, столиками и заказами!

