# Техническое задание по дизайну — Dámdi QR

## 1. Общие принципы дизайна

### 1.1. Философия дизайна
Dámdi QR использует современный минималистичный дизайн с акцентом на:
- **Чистота и простота** — понятный интерфейс без лишних элементов
- **Профессионализм** — деловой стиль для административных панелей
- **Дружелюбность** — теплые оттенки для клиентской части
- **Доступность** — четкая типографика и контрастность
- **Мобильность** — приоритет мобильного опыта

### 1.2. Стиль
- **Направление:** Modern Flat Design с мягкими тенями
- **Подход:** Mobile-first, затем адаптация для планшетов и десктопов
- **Визуальный язык:** Иконки + текст, карточки, модальные окна

## 2. Цветовая палитра

### 2.1. Основные цвета

#### Primary (Основной брендовый цвет)
- **Orange Primary:** `#F37321`
  - Использование: CTA-кнопки, акценты, логотипы, активные состояния
  - Hover: `#e0681a` (темнее на 10%)
  - RGB: `rgb(243, 115, 33)`

#### Secondary цвета
- **Dark Blue:** `#1E2A3A`
  - Использование: футеры, темные кнопки, важные заголовки
  - RGB: `rgb(30, 42, 58)`

#### Акцентные цвета
- **Green Success/Price:** `#34A853`
  - Использование: цены, успешные статусы, галочки
  - RGB: `rgb(52, 168, 83)`

- **Orange Light BG:** `#FFF5EB`
  - Использование: фоновые акценты, светлые блоки
  - RGB: `rgb(255, 245, 235)`

### 2.2. Нейтральные цвета

#### Фоны
- **Белый:** `#FFFFFF`
  - Использование: основной фон страниц, карточки
  - RGB: `rgb(255, 255, 255)`

- **Light Gray BG:** `#F7F8FC`
  - Использование: фоны секций, панелей управления
  - RGB: `rgb(247, 248, 252)`

- **Nav BG:** `#F0F1F5`
  - Использование: фон навигации
  - RGB: `rgb(240, 241, 245)`

- **Search BG:** `#F4F4F4`
  - Использование: фон поиска
  - RGB: `rgb(244, 244, 244)`

- **Warm Light BG (Client Menu):** `#FFFBF7`
  - Использование: фон клиентского меню (теплый оттенок)
  - RGB: `rgb(255, 251, 247)`

#### Текст
- **Text Dark:** `#222222`
  - Использование: основной текст, заголовки
  - RGB: `rgb(34, 34, 34)`

- **Text Secondary:** `#555555` (Landing/Admin) или `#888888` (Client Menu)
  - Использование: вторичный текст, описания, подсказки
  - RGB: `rgb(85, 85, 85)` / `rgb(136, 136, 136)`

- **Text Light:** `#FFFFFF`
  - Использование: текст на цветных фонах
  - RGB: `rgb(255, 255, 255)`

#### Границы
- **Border Color:** `#ECEEF3`
  - Использование: границы карточек, разделители, инпуты
  - RGB: `rgb(236, 238, 243)`

### 2.3. Статусные цвета

#### Success (Успех)
- Основной: `#34A853` (Green Success)
- Использование: успешные операции, положительные индикаторы

#### Warning (Предупреждение)
- Основной: `#F59E0B`
- Использование: предупреждения, важные уведомления

#### Danger (Ошибка)
- Основной: `#EF4444`
- Использование: ошибки, удаление, критические действия

### 2.4. Правила использования цветов

1. **Primary Orange** — только для интерактивных элементов (кнопки, ссылки, акценты)
2. **Dark Blue** — для серьезных блоков (футер, важные секции)
3. **Green** — только для цен и положительных статусов
4. **Нейтральные** — для фонов и текста, никогда для акцентов
5. **Контрастность** — минимальный контраст 4.5:1 для текста

## 3. Типографика

### 3.1. Семейство шрифтов

**Основной шрифт (System Font Stack):**
```css
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
```

**Принцип:** Использование системных шрифтов для:
- Быстрой загрузки (без загрузки внешних шрифтов)
- Нативного вида в разных ОС
- Отличной читаемости

### 3.2. Размеры шрифтов

#### Заголовки
- **H1:** `48px` (Landing) / `32px` (Dashboard)
  - Font-weight: `700`
  - Line-height: `1.2`
  - Использование: главные заголовки страниц

- **H2:** `36px` (Landing Desktop) / `28px` (Tablet) / `24px` (Mobile)
  - Font-weight: `700`
  - Line-height: `1.3`
  - Использование: заголовки секций

- **H3:** `30px` / `24px` / `20px`
  - Font-weight: `700`
  - Использование: подзаголовки, заголовки карточек

#### Основной текст
- **Body Large:** `18px`
  - Использование: важные описания, hero-текст
  - Line-height: `1.6`

- **Body Regular:** `16px`
  - Использование: основной текст, параграфы
  - Line-height: `1.6`

- **Body Small:** `14px`
  - Использование: подсказки, метаинформация, вторичный текст
  - Line-height: `1.5`

- **Body Tiny:** `12px`
  - Использование: мелкие примечания, даты

### 3.3. Начертания шрифтов

- **Regular (400):** основной текст
- **Medium (600):** кнопки, важные элементы
- **Bold (700):** заголовки, акценты

### 3.4. Иерархия текста

1. **Уровень 1 (H1):** Самый важный — привлечение внимания
2. **Уровень 2 (H2):** Структура страницы — разделы
3. **Уровень 3 (H3):** Подразделы — карточки
4. **Body:** Основной контент
5. **Secondary:** Дополнительная информация

## 4. Компоненты UI

### 4.1. Кнопки

#### Primary Button (Основная)
```css
background-color: #F37321;
color: #FFFFFF;
border: 2px solid #F37321;
border-radius: 8px;
padding: 14px 30px;
font-weight: 600;
font-size: 16px;
```
- **Hover:** `background-color: #e0681a`
- **Disabled:** `opacity: 0.5`
- Использование: главные действия (Создать, Сохранить, Отправить)

#### Secondary Button (Вторичная)
```css
background-color: #FFFFFF;
color: #222222;
border: 2px solid #ECEEF3;
border-radius: 8px;
padding: 14px 30px;
font-weight: 600;
```
- **Hover:** `border-color: #222222`
- Использование: отмена, второстепенные действия

#### Dark Button (Темная)
```css
background-color: #1E2A3A;
color: #FFFFFF;
border: 2px solid #1E2A3A;
```
- Использование: важные, но не основные действия

#### Large Button
```css
padding: 16px 40px;
font-size: 16px;
```
- Использование: Hero-секции, важные CTA

### 4.2. Инпуты и формы

#### Text Input
```css
width: 100%;
padding: 10px 12px;
border: 1px solid #ECEEF3;
border-radius: 8px;
font-size: 16px;
font-family: inherit;
background-color: #FFFFFF;
color: #222222;
```

**Состояния:**
- **Default:** `border-color: #ECEEF3`
- **Focus:** `border-color: #F37321`, `outline: none`
- **Error:** `border-color: #EF4444`
- **Disabled:** `background-color: #F7F8FC`, `cursor: not-allowed`

#### Textarea
```css
/* Те же стили, что и input */
resize: vertical;
min-height: 80px;
font-family: inherit;
```

#### Label
```css
display: block;
margin-bottom: 5px;
font-weight: 600;
font-size: 14px;
color: #222222;
```

#### Placeholder
```css
color: #888888;
opacity: 0.7;
```

### 4.3. Карточки

#### Basic Card
```css
background-color: #FFFFFF;
border: 1px solid #ECEEF3;
border-radius: 12px;
padding: 30px;
box-shadow: 0 4px 15px rgba(220, 220, 230, 0.5);
```

**Варианты:**
- **Feature Card:** текст по центру, иконка сверху
- **Price Card:** для тарифов, с тенью и акцентом
- **Testimonial Card:** отзывы клиентов

#### Client Menu Card (Dish Card)
```css
background-color: #FFFFFF;
border-radius: 16px;
padding: 15px;
box-shadow: 0 4px 15px rgba(200, 180, 170, 0.4);
```

### 4.4. Модальные окна

#### Modal Overlay
```css
position: fixed;
top: 0;
left: 0;
right: 0;
bottom: 0;
background-color: rgba(0, 0, 0, 0.5);
backdrop-filter: blur(4px);
z-index: 1000;
display: flex;
align-items: center;
justify-content: center;
```

#### Modal Content
```css
background-color: #FFFFFF;
border-radius: 12px;
max-width: 500px;
width: 90%;
max-height: 90vh;
overflow-y: auto;
box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
```

#### Modal Header
```css
display: flex;
justify-content: space-between;
align-items: center;
padding: 20px;
border-bottom: 1px solid #ECEEF3;
```

#### Close Button
```css
font-size: 32px;
line-height: 1;
color: #888888;
cursor: pointer;
border: none;
background: none;
padding: 0;
width: 32px;
height: 32px;
```

### 4.5. Навигация

#### Tab Navigation
```css
background-color: #F0F1F5;
border-radius: 8px;
padding: 5px;
display: flex;
gap: 5px;
```

#### Tab Item
```css
padding: 10px 20px;
border-radius: 8px;
font-weight: 600;
cursor: pointer;
transition: all 0.2s;
color: #222222;
background-color: transparent;
```

#### Tab Item Active
```css
background-color: #333333;
color: #FFFFFF;
```

### 4.6. Badge / Tag

#### Tag Gray
```css
background-color: #E0E0E0;
color: #555555;
font-size: 12px;
font-weight: 700;
padding: 6px 12px;
border-radius: 20px;
```

#### Tag Orange
```css
background-color: #F37321;
color: #FFFFFF;
font-size: 12px;
font-weight: 700;
padding: 6px 12px;
border-radius: 20px;
```

### 4.7. Аватары и иконки

#### Logo Icon
```css
width: 40px;
height: 40px;
background-color: #F37321;
border-radius: 8px; /* или 50% для круглого */
display: flex;
align-items: center;
justify-content: center;
color: #FFFFFF;
font-weight: bold;
```

#### Feature Icon
```css
width: 56px;
height: 56px;
border-radius: 50%;
display: flex;
align-items: center;
justify-content: center;
font-size: 24px;
```

### 4.8. Списки и таблицы

#### Order List Item
```css
padding: 15px;
border-bottom: 1px solid #ECEEF3;
display: grid;
grid-template-columns: auto 1fr auto;
gap: 15px;
align-items: center;
```

#### Table
```css
width: 100%;
border-collapse: collapse;
```

#### Table Header
```css
background-color: #F7F8FC;
font-weight: 600;
text-align: left;
padding: 12px;
border-bottom: 2px solid #ECEEF3;
```

#### Table Cell
```css
padding: 12px;
border-bottom: 1px solid #ECEEF3;
```

## 5. Специфичные страницы

### 5.1. Лендинг (Landing Page)

#### Hero Section
- Фон: `#FFFFFF`
- Заголовок: H1, `48px` (desktop) / `28px` (mobile)
- CTA-кнопки: центрированы, `btn-large`
- Social Proof: горизонтально (desktop) / вертикально (mobile)

#### Features Section
- Фон: `#F7F8FC`
- Сетка: 3 колонки (desktop) / 2 колонки (tablet) / 1 колонка (mobile)
- Карточки: белые, с тенью, иконка сверху

#### Steps Section
- Иконки: круглые, `80px`, оранжевый фон
- Сетка: 4 колонки (desktop) / 2 колонки (tablet) / 1 колонка (mobile)

#### Pricing Section
- Фон: `#F7F8FC`
- Сетка: 2 колонки (desktop) / 1 колонка (mobile)
- Popular card: `scale(1.05)`, оранжевая граница

#### Footer
- Фон: `#1E2A3A`
- Текст: `#CCCCCC`
- Сетка: 4 колонки (desktop) / 2 колонки (tablet) / 1 колонка (mobile)

### 5.2. Клиентское меню (Client Menu)

#### Общий стиль
- Фон: `#FFFBF7` (теплый бежевый)
- Максимальная ширина: `420px`
- Центрирование с тенью по бокам

#### Header
- Sticky позиционирование
- Название ресторана: оранжевый, `24px`, `font-weight: 700`
- Поиск: серый фон `#F4F4F4`

#### Category Navigation
- Горизонтальный скролл
- Sticky позиционирование
- Активная категория: оранжевый текст

#### Dish Cards
- Сетка: 2 колонки
- Изображение: `150px` высота, закругленные углы
- Цена: зеленый цвет `#34A853`

#### Cart Button
- Fixed внизу экрана
- Оранжевый фон
- Показывается только при наличии товаров в корзине

#### Order Type Toggle
- Две кнопки рядом
- Активная: оранжевый фон, белый текст
- Неактивная: белый фон, темный текст

### 5.3. Админ-панель (Admin Dashboard)

#### Layout
- Фон: `#F7F8FC`
- Header: белый, фиксированный
- Navigation: вертикальное меню, серый фон `#F0F1F5`
- Content: белый фон карточек

#### Statistics Cards
- Сетка: 3-4 колонки (desktop) / 2 колонки (tablet) / 1 колонка (mobile)
- Фон: белый
- Цифры: крупный шрифт `32px`
- Подпись: серый текст

#### Forms
- Все инпуты: единый стиль
- Кнопки действий: справа внизу формы

#### Tables List
- Карточки вместо таблиц
- Сетка: адаптивная

### 5.4. Панель сотрудника (Staff Dashboard)

#### Kanban Board
- Фон: `#F7F8FC`
- Колонки: 4 (NEW, PREPARING, READY, COMPLETED)
- Карточки заказов: белые, с тенью
- Drag & Drop стили (при наведении)

#### Order Card
- Номер столика: крупно, оранжевый
- Список блюд: обычный текст
- Сумма: зеленый, `font-weight: 700`
- Кнопка статуса: внизу карточки

### 5.5. Панель супер-админа (Super Admin Dashboard)

#### Similar to Admin Dashboard
- Та же структура
- Дополнительные фильтры для ресторанов
- Таблица со всеми ресторанами

## 6. Тени и эффекты

### 6.1. Тени

#### Card Shadow
```css
box-shadow: 0 4px 15px rgba(220, 220, 230, 0.5);
```

#### Large Shadow
```css
box-shadow: 0 5px 25px rgba(220, 220, 230, 0.6);
```

#### Client Menu Shadow
```css
box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
```

#### Modal Shadow
```css
box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
```

### 6.2. Скругления

- **Small:** `8px` — кнопки, инпуты
- **Medium:** `12px` — карточки, модалки
- **Large:** `16px` — карточки клиентского меню
- **Circle:** `50%` — аватары, иконки

### 6.3. Transitions

Все интерактивные элементы:
```css
transition: all 0.2s ease;
```

Более плавные анимации:
```css
transition: all 0.3s ease;
```

## 7. Адаптивность

### 7.1. Breakpoints

#### Mobile
- **Максимальная ширина:** `480px`
- Особенности:
  - Одна колонка для всех сеток
  - Вертикальное расположение кнопок
  - Уменьшенные отступы
  - Sticky навигация

#### Tablet
- **Максимальная ширина:** `768px`
- Особенности:
  - 2 колонки для карточек
  - Средние отступы
  - Горизонтальная навигация

#### Desktop
- **Минимальная ширина:** `769px`
- Особенности:
  - 3-4 колонки для карточек
  - Максимальная ширина контента: `1200px`
  - Боковая навигация

### 7.2. Mobile-First подход

1. Сначала стили для мобильных
2. Затем `@media (min-width: 481px)` для планшетов
3. Затем `@media (min-width: 769px)` для десктопов

### 7.3. Адаптивные паттерны

#### Grid → Stack
- Desktop: `grid-template-columns: repeat(3, 1fr)`
- Tablet: `grid-template-columns: repeat(2, 1fr)`
- Mobile: `grid-template-columns: 1fr`

#### Horizontal → Vertical
- Desktop: `flex-direction: row`
- Mobile: `flex-direction: column`

#### Fixed Width → Full Width
- Desktop: `max-width: 420px` (Client Menu)
- Mobile: `width: 100%`

## 8. Иконки и изображения

### 8.1. Иконки

#### Эмодзи (текущий подход)
- Использование эмодзи для иконок
- Размер: `24px` - `32px`
- Примеры: 📱, 👍, ⏱, 📈, 🛡️, 📊

#### Альтернатива (будущее)
- Иконочный шрифт (Font Awesome, Material Icons)
- SVG-спрайты
- React Icons библиотека

### 8.2. Изображения

#### Блюда
- Формат: WebP (с fallback на JPG)
- Размер: оптимизация до `800px` по ширине
- Соотношение сторон: `4:3` или `16:9`
- Скругление: `12px`

#### Логотип
- Размер: `40px × 40px`
- Формат: SVG (предпочтительно) или PNG
- Фон: оранжевый `#F37321`

### 8.3. Placeholder

#### Для изображений блюд
```css
background-color: #F7F8FC;
border: 1px dashed #ECEEF3;
display: flex;
align-items: center;
justify-content: center;
color: #888888;
```

## 9. Состояния элементов

### 9.1. Интерактивные состояния

#### Hover
- Кнопки: изменение цвета фона (темнее на 10%)
- Ссылки: подчеркивание или изменение цвета
- Карточки: легкое увеличение тени

#### Active
- Кнопки: небольшое уменьшение размера (`scale(0.98)`)
- Tab: изменение фона

#### Focus
- Инпуты: оранжевая граница
- Кнопки: outline (для доступности)

#### Disabled
- Opacity: `0.5`
- Cursor: `not-allowed`
- Нет hover-эффектов

### 9.2. Loading состояния

#### Skeleton Loader
```css
background: linear-gradient(90deg, #F7F8FC 25%, #FFFFFF 50%, #F7F8FC 75%);
background-size: 200% 100%;
animation: loading 1.5s infinite;
```

#### Spinner
- Круговой индикатор
- Цвет: оранжевый `#F37321`
- Размер: `40px × 40px`

### 9.3. Ошибки и валидация

#### Error Message
```css
color: #EF4444;
font-size: 14px;
margin-top: 5px;
```

#### Error Input
```css
border-color: #EF4444;
```

#### Success Message
```css
color: #34A853;
background-color: rgba(52, 168, 83, 0.1);
padding: 10px;
border-radius: 8px;
```

## 10. Доступность (A11y)

### 10.1. Контрастность

- Основной текст: минимум `4.5:1` (WCAG AA)
- Заголовки: минимум `3:1` (WCAG AA)
- Интерактивные элементы: минимум `3:1` (WCAG AA)

### 10.2. Фокус

- Видимый outline для всех интерактивных элементов
- Цвет: оранжевый `#F37321`
- Толщина: `2px`

### 10.3. Клавиатурная навигация

- Все интерактивные элементы доступны через Tab
- Логический порядок Tab-индекса
- Enter/Space для активации кнопок

### 10.4. Screen Readers

- Alt-тексты для всех изображений
- ARIA-labels для иконок без текста
- Семантические HTML-теги (header, nav, main, footer)

## 11. Анимации

### 11.1. Переходы

#### Fade In
```css
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(-10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

#### Slide In
```css
@keyframes slideIn {
  from {
    transform: translateX(-20px);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}
```

### 11.2. Микро-анимации

- Hover на кнопках: `0.2s`
- Открытие модалки: `0.3s`
- FAQ аккордеон: `0.3s`

### 11.3. Правила

- Не более `300ms` для UI-переходов
- `ease` timing function для плавности
- Избегать анимаций для пользователей с `prefers-reduced-motion`

## 12. Правила именования CSS классов

### 12.1. БЭМ-подобная структура

```
.block
.block__element
.block--modifier
```

Примеры:
- `.landing-page`
- `.landing-page__hero`
- `.landing-page__hero--large`
- `.btn`
- `.btn--primary`
- `.btn--large`

### 12.2. Компонентный подход

- Каждый компонент/страница имеет свой CSS файл
- Глобальные переменные в `:root`
- Использование CSS-переменных для цветов

## 13. Руководство по использованию

### 13.1. Когда использовать Primary Orange

✅ **Используйте для:**
- Главные CTA-кнопки
- Активные состояния
- Акценты и выделения
- Логотипы и брендинг

❌ **Не используйте для:**
- Обычного текста
- Фонов (кроме акцентов)
- Ошибок и предупреждений

### 13.2. Когда использовать Green

✅ **Используйте для:**
- Цены товаров
- Успешные операции
- Положительные статусы

❌ **Не используйте для:**
- Кнопок действий
- Основных акцентов

### 13.3. Правила типографики

- **Один заголовок H1** на странице
- **Последовательная иерархия** (H1 → H2 → H3)
- **Не смешивайте** размеры шрифтов без причины
- **Line-height:** минимум `1.5` для читаемости

### 13.4. Правила отступов

- **Базовый отступ:** `8px`
- **Множители:** `8px, 16px, 24px, 32px, 40px, 48px`
- **Консистентность:** одинаковые отступы в похожих элементах

## 14. Примеры использования

### 14.1. Кнопка CTA

```html
<button class="btn btn-primary btn-large">
  Начать бесплатно
</button>
```

### 14.2. Карточка функции

```html
<div class="feature-card">
  <div class="feature-icon">📱</div>
  <h3>QR-меню за 5 минут</h3>
  <p>Описание функции</p>
</div>
```

### 14.3. Форма

```html
<div class="form-group">
  <label>Название</label>
  <input type="text" class="form-input" />
  <span class="form-error">Ошибка валидации</span>
</div>
```

## 15. Чеклист для дизайнера/разработчика

### Перед релизом проверьте:

- [ ] Все цвета соответствуют палитре
- [ ] Типографика соответствует руководству
- [ ] Адаптивность на всех breakpoints
- [ ] Контрастность соответствует WCAG AA
- [ ] Все интерактивные элементы имеют hover-состояния
- [ ] Формы имеют валидацию и error-состояния
- [ ] Loading-состояния для всех асинхронных действий
- [ ] Модальные окна закрываются по Esc и клику вне окна
- [ ] Все изображения имеют alt-тексты
- [ ] Фокус виден для всех элементов

---

**Версия документа:** 1.0  
**Дата создания:** 2025-11-22  
**Последнее обновление:** 2025-11-22

