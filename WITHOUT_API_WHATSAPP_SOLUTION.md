# 🔧 БЫСТРАЯ НАСТРОЙКА АВТОМАТИЧЕСКОЙ ОТПРАВКИ В WHATSAPP

## ✅ Вариант 1: Green API (БЕСПЛАТНО для начала, популярно в СНГ)

### Шаги (5 минут):

1. **Зарегистрируйся на https://green-api.com** (бесплатно)
2. **Получи данные:**
   - `idInstance` - ID твоего инстанса
   - `apiTokenInstance` - токен доступа

3. **Добавь в `nodejs/.env`:**
   ```env
   GREEN_API_ID=твой_idInstance
   GREEN_API_TOKEN=твой_apiTokenInstance
   ```

4. **Перезапусти backend:**
   ```bash
   cd nodejs && npm run dev
   ```

5. **Готово!** Теперь все заказы автоматически отправляются в WhatsApp.

---

## ✅ Вариант 2: Twilio WhatsApp API (платный)

1. Зарегистрируйся на https://www.twilio.com
2. Получи `Account SID`, `Auth Token` и номер WhatsApp
3. Добавь в `nodejs/.env`:
   ```env
   TWILIO_ACCOUNT_SID=твой_sid
   TWILIO_AUTH_TOKEN=твой_token
   TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
   ```

---

## ⚠️ Без API автоматическая отправка НЕВОЗМОЖНА

WhatsApp не позволяет отправлять сообщения автоматически без официального API.
Для автоматической отправки ОБЯЗАТЕЛЬНО нужен один из сервисов выше.

