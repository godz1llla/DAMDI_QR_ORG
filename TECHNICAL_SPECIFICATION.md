# Техническое задание проекта Dámdi QR

## 1. Общие сведения

### 1.1. Назначение проекта
Dámdi QR — это SaaS-платформа для управления ресторанами с функционалом QR-меню, системой заказов и интеграцией с мессенджерами. Платформа позволяет ресторанам создавать цифровые меню, генерировать QR-коды для столиков, принимать заказы от гостей и управлять персоналом.

### 1.2. Целевая аудитория
- **Владельцы ресторанов, кафе, баров** — основная целевая аудитория
- **Официанты и сотрудники заведений** — для обработки заказов
- **Гости ресторанов** — для просмотра меню и оформления заказов

### 1.3. Технологический стек

#### Backend
- **Node.js** — серверная платформа
- **Express.js** — веб-фреймворк
- **TypeScript** — язык программирования
- **MySQL** — реляционная база данных
- **JWT** — аутентификация
- **Docker** — контейнеризация MySQL

#### Frontend
- **React 18** — библиотека для UI
- **TypeScript** — язык программирования
- **Vite** — сборщик и dev-сервер
- **React Router** — маршрутизация
- **Axios** — HTTP-клиент
- **CSS Modules** — стилизация

## 2. Функциональные требования

### 2.1. Публичные страницы

#### 2.1.1. Лендинг (Landing Page)
**Описание:** Маркетинговая страница для привлечения новых клиентов.

**Функционал:**
- Hero-секция с призывом к действию
- Раздел с преимуществами платформы (Features)
- Описание процесса запуска в 4 шагах
- Раздел с тарифами (Бесплатный / Premium)
- Отзывы клиентов (Testimonials)
- FAQ (аккордеон с ответами на вопросы)
- Финальный CTA-блок
- Футер с контактами и ссылками
- Адаптивный дизайн для мобильных, планшетов и десктопов

**URL:** `/`

#### 2.1.2. Страница входа (Login Page)
**Описание:** Страница авторизации для владельцев, сотрудников и супер-администраторов.

**Функционал:**
- Форма входа (email + пароль)
- Кнопки быстрого входа для демо-аккаунтов
- Валидация полей
- Отображение ошибок авторизации
- Редирект после успешного входа в зависимости от роли

**URL:** `/login`

#### 2.1.3. Клиентское меню (Client Menu)
**Описание:** Публичная страница с меню ресторана для гостей.

**Функционал:**
- Просмотр меню ресторана (категории и блюда)
- Поиск по названию блюда
- Фильтрация по категориям
- Добавление блюд в корзину
- Управление количеством товаров в корзине
- Переключатель типа заказа: "В ресторане" / "Доставка"
- Модальное окно корзины с итоговой суммой
- Модальное окно для данных доставки (адрес, телефон)
- Модальное окно подтверждения заказа с кнопкой WhatsApp
- Отправка заказа через API
- Адаптивный мобильный дизайн

**URL:** `/menu?restaurant_id={id}&table_id={id}`

**Типы заказов:**
- **DINE_IN** (В ресторане) — заказ привязан к столику
- **DELIVERY** (Доставка) — заказ требует адрес и телефон

### 2.2. Административные панели

#### 2.2.1. Панель супер-администратора (Super Admin Dashboard)
**Описание:** Панель для управления всеми ресторанами в системе.

**Функционал:**
- **Главная (Home):**
  - Статистика: общее количество ресторанов, активных ресторанов, заказов за сегодня
  - Список последних заказов со всех ресторанов
- **Рестораны (Restaurants):**
  - Список всех ресторанов с фильтрами
  - Создание нового ресторана с данными владельца
  - Изменение статуса активности ресторана
  - Изменение тарифного плана (FREE / PREMIUM)
  - Удаление ресторана

**Роль:** `SUPER_ADMIN`

**URL:** `/dashboard/super-admin`

#### 2.2.2. Панель администратора ресторана (Admin Dashboard)
**Описание:** Основная панель для владельцев ресторанов.

**Функционал:**
- **Главная (Home):**
  - Статистика: заказов сегодня, заказов в месяц, выручка сегодня, выручка в месяц
  - График выручки (сегодня, неделя, месяц, год)
  - Список последних заказов
- **Меню (Menu):**
  - Создание и редактирование категорий меню
  - Добавление блюд с фото, названием, описанием, ценой
  - Загрузка изображений блюд (автоматическая оптимизация)
  - Управление доступностью блюд
  - Удаление категорий и блюд
  - Ограничения по тарифу (FREE: до 5 категорий, PREMIUM: неограниченно)
- **Столики (Tables):**
  - Создание столиков с номерами
  - Генерация QR-кодов для столиков
  - Превью QR-кода
  - Скачивание QR-кода (PNG)
  - Удаление столиков
  - Ограничения по тарифу (FREE: до 10 столиков, PREMIUM: до 999)
- **Сотрудники (Staff):**
  - Список сотрудников ресторана
  - Добавление нового сотрудника (email, имя, фамилия, пароль)
  - Удаление сотрудника
- **Настройки (Settings):**
  - Редактирование названия ресторана
  - Редактирование адреса
  - Редактирование телефона
  - **Редактирование WhatsApp номера** (для уведомлений о заказах доставки)
  - Отображение текущего тарифного плана

**Роль:** `ADMIN`

**URL:** `/dashboard/admin`

#### 2.2.3. Панель сотрудника (Staff Dashboard)
**Описание:** Доска заказов в стиле Kanban для официантов.

**Функционал:**
- **Kanban-доска с колонками:**
  - **Новые заказы** (NEW) — только заказы типа DINE_IN
  - **В процессе** (PREPARING) — только заказы типа DINE_IN
  - **Готово** (READY) — только заказы типа DINE_IN
  - **Выполнено** (COMPLETED) — только заказы типа DINE_IN
- **Карточка заказа:**
  - Номер заказа
  - Номер столика
  - Список блюд с количеством
  - Итоговая сумма
  - Время создания заказа
  - Кнопка изменения статуса
- **Real-time обновления:**
  - Автоматическое обновление каждые 3 секунды (polling)
  - Появление новых заказов без перезагрузки страницы
  - Фильтрация: только заказы типа DINE_IN (заказы DELIVERY не отображаются)

**Роль:** `STAFF`

**URL:** `/dashboard/staff`

**Важно:** Заказы типа DELIVERY не отображаются на доске сотрудника, они уходят только в WhatsApp.

### 2.3. Система заказов

#### 2.3.1. Создание заказа
**Процесс:**
1. Гость сканирует QR-код на столике или открывает ссылку
2. Просматривает меню, добавляет блюда в корзину
3. Выбирает тип заказа: "В ресторане" или "Доставка"
4. Нажимает "Оформить заказ"
5. Если выбран "Доставка", появляется модалка с полями:
   - Телефон клиента (обязательное)
   - Адрес доставки (обязательное)
6. После заполнения — подтверждение заказа
7. Заказ сохраняется в базу данных

**Данные заказа:**
- `restaurant_id` — ID ресторана
- `table_id` — ID столика (NULL для доставки)
- `order_type` — тип заказа (DINE_IN / DELIVERY)
- `customer_phone` — телефон клиента (для DELIVERY)
- `delivery_address` — адрес доставки (для DELIVERY)
- `status` — статус заказа (NEW, PREPARING, READY, COMPLETED)
- `total_amount` — итоговая сумма
- `items` — список блюд с количеством и ценой

#### 2.3.2. Обработка заказов
- **DINE_IN заказы:**
  - Появляются на доске сотрудника
  - Обрабатываются через Kanban-доску
  - Переход между статусами: NEW → PREPARING → READY → COMPLETED

- **DELIVERY заказы:**
  - **Не появляются** на доске сотрудника
  - Отправляются **только** в WhatsApp владельцу ресторана
  - Модальное окно с кнопкой "Открыть в WhatsApp" для клиента

#### 2.3.3. Уведомления в WhatsApp
**Для заказов доставки:**
- После создания заказа генерируется сообщение с:
  - Номером заказа
  - Типом заказа (🚚 ДОСТАВКА)
  - Суммой заказа
  - Датой и временем
  - Телефоном клиента
  - Адресом доставки
  - Списком блюд с количеством и ценами
  - Итоговой суммой
- Сообщение отправляется на WhatsApp номер ресторана (из настроек)
- **Варианты отправки:**
  1. Автоматически через Green API (если настроено в `.env`)
  2. Автоматически через Twilio (если настроено в `.env`)
  3. Модальное окно с кнопкой "Открыть в WhatsApp" (wa.me ссылка)

**Формат сообщения:**
```
📦 *НОВЫЙ ЗАКАЗ #123* (от Название Ресторана)

🚚 ДОСТАВКА
💰 Сумма: 5 500 ₸
🕐 22.11.2025, 17:30

📞 Телефон: +7 777 123-45-67
📍 Адрес: ул. Примерная, д. 1, кв. 5

*Состав заказа:*
1. Бургер с помидорами x2 = 3 000 ₸
2. Картошка фри x1 = 1 500 ₸
3. Кола x1 = 1 000 ₸

💵 *Итого: 5 500 ₸*
```

### 2.4. Система аутентификации и авторизации

#### 2.4.1. Роли пользователей
1. **SUPER_ADMIN**
   - Управление всеми ресторанами
   - Создание ресторанов
   - Изменение тарифов
   - Доступ ко всем заказам

2. **ADMIN** (владелец ресторана)
   - Управление своим рестораном
   - Управление меню, столиками, сотрудниками
   - Просмотр статистики и заказов
   - Настройка ресторана

3. **STAFF** (сотрудник)
   - Доступ к доске заказов (только DINE_IN)
   - Изменение статусов заказов

#### 2.4.2. JWT-токены
- Токен содержит: `userId`, `email`, `role`, `restaurantId`
- Время жизни: 7 дней (настраивается в `.env`)
- Хранение: `localStorage` в браузере
- Автоматическое добавление в заголовок `Authorization: Bearer {token}`

#### 2.4.3. Защита маршрутов
- Публичные: `/`, `/login`, `/menu`
- Защищенные: `/dashboard/*`
- Проверка роли для доступа к определенным панелям

### 2.5. Тарифная система

#### 2.5.1. Тарифы

**FREE (Бесплатный)**
- До 10 столиков
- До 5 категорий меню
- Неограниченное количество блюд
- Базовая аналитика
- Email поддержка

**PREMIUM (Платный)**
- До 999 столиков
- Неограниченное количество категорий
- Неограниченное количество блюд
- Расширенная аналитика
- Приоритетная поддержка 24/7
- Персональный менеджер
- Кастомизация дизайна
- API для интеграций

#### 2.5.2. Ограничения
- Проверка лимитов при создании категорий и столиков
- API возвращает ошибку при превышении лимита
- Отображение текущего тарифа в настройках

### 2.6. QR-коды

#### 2.6.1. Генерация QR-кодов
- Генерация для каждого столика
- Ссылка в QR-коде: `{FRONTEND_URL}/menu?restaurant_id={id}&table_id={id}`
- Формат: PNG изображение
- Размер: настраиваемый (по умолчанию 512x512px)

#### 2.6.2. Превью и скачивание
- Превью QR-кода в админ-панели
- Скачивание QR-кода для печати
- Возможность регенерации при необходимости

## 3. Нефункциональные требования

### 3.1. Производительность
- Время загрузки страниц < 2 секунд
- Real-time обновления заказов каждые 3 секунды
- Оптимизация изображений (WebP, ресайз)

### 3.2. Безопасность
- JWT-аутентификация
- Хеширование паролей (bcrypt)
- Валидация входных данных
- Защита от SQL-инъекций (prepared statements)
- CORS-настройки

### 3.3. Масштабируемость
- REST API для будущих интеграций
- Модульная архитектура
- Готовность к горизонтальному масштабированию

### 3.4. Совместимость
- Поддержка современных браузеров (Chrome, Firefox, Safari, Edge)
- Адаптивный дизайн для мобильных, планшетов и десктопов
- Кодировка UTF-8 для корректного отображения казахского и русского языков

### 3.5. Удобство использования
- Интуитивный интерфейс
- Мобильная оптимизация клиентского меню
- Быстрая навигация
- Понятные сообщения об ошибках

## 4. Структура базы данных

### 4.1. Основные таблицы

**users**
- `id` — первичный ключ
- `email` — email пользователя (уникальный)
- `password_hash` — хеш пароля
- `first_name` — имя
- `last_name` — фамилия
- `role` — роль (SUPER_ADMIN, ADMIN, STAFF)
- `restaurant_id` — ID ресторана (NULL для SUPER_ADMIN)
- `created_at`, `updated_at` — временные метки

**restaurants**
- `id` — первичный ключ
- `name` — название ресторана
- `address` — адрес
- `phone` — телефон
- `whatsapp_number` — WhatsApp номер для уведомлений
- `owner_id` — ID владельца (может быть NULL)
- `plan` — тарифный план (FREE, PREMIUM)
- `is_active` — активен ли ресторан
- `created_at`, `updated_at` — временные метки

**menu_categories**
- `id` — первичный ключ
- `restaurant_id` — ID ресторана
- `name` — название категории
- `display_order` — порядок отображения
- `created_at`, `updated_at` — временные метки

**menu_items**
- `id` — первичный ключ
- `restaurant_id` — ID ресторана
- `category_id` — ID категории
- `name` — название блюда
- `description` — описание
- `price` — цена
- `image_url` — ссылка на изображение
- `is_available` — доступно ли блюдо
- `display_order` — порядок отображения
- `created_at`, `updated_at` — временные метки

**tables**
- `id` — первичный ключ
- `restaurant_id` — ID ресторана
- `table_number` — номер столика
- `qr_code_url` — ссылка на QR-код
- `created_at`, `updated_at` — временные метки

**orders**
- `id` — первичный ключ
- `restaurant_id` — ID ресторана
- `table_id` — ID столика (NULL для доставки)
- `order_type` — тип заказа (DINE_IN, DELIVERY)
- `customer_phone` — телефон клиента (для доставки)
- `delivery_address` — адрес доставки (для доставки)
- `status` — статус (NEW, PREPARING, READY, COMPLETED)
- `total_amount` — итоговая сумма
- `created_at`, `updated_at` — временные метки

**order_items**
- `id` — первичный ключ
- `order_id` — ID заказа
- `menu_item_id` — ID блюда
- `quantity` — количество
- `price` — цена на момент заказа
- `notes` — заметки
- `created_at` — временная метка

## 5. API Endpoints

### 5.1. Аутентификация
- `POST /api/auth/login` — вход
- `GET /api/auth/me` — текущий пользователь
- `POST /api/auth/logout` — выход

### 5.2. Рестораны
- `POST /api/restaurants` — создать ресторан (SUPER_ADMIN)
- `GET /api/restaurants` — список ресторанов (SUPER_ADMIN)
- `GET /api/restaurants/stats` — статистика (SUPER_ADMIN)
- `GET /api/restaurants/:id/public` — публичная информация о ресторане
- `GET /api/restaurants/my` — мой ресторан (ADMIN)
- `PUT /api/restaurants/my` — обновить ресторан (ADMIN)
- `PUT /api/restaurants/:id` — обновить ресторан (SUPER_ADMIN)
- `DELETE /api/restaurants/:id` — удалить ресторан (SUPER_ADMIN)
- `GET /api/restaurants/limits` — лимиты тарифа (ADMIN)

### 5.3. Меню
- `GET /api/menu?restaurant_id={id}` — получить меню
- `POST /api/menu/categories` — создать категорию (ADMIN)
- `POST /api/menu/items` — создать блюдо (ADMIN)
- `PUT /api/menu/items/:id` — обновить блюдо (ADMIN)
- `DELETE /api/menu/categories/:id` — удалить категорию (ADMIN)
- `DELETE /api/menu/items/:id` — удалить блюдо (ADMIN)

### 5.4. Столики
- `GET /api/tables` — список столиков (ADMIN)
- `POST /api/tables` — создать столик (ADMIN)
- `DELETE /api/tables/:id` — удалить столик (ADMIN)

### 5.5. Заказы
- `POST /api/orders` — создать заказ
- `GET /api/orders` — список заказов (ADMIN/STAFF/SUPER_ADMIN)
- `GET /api/orders/poll?last_id={id}` — новые заказы (STAFF, только DINE_IN)
- `PUT /api/orders/:id/status` — обновить статус (STAFF)

### 5.6. Сотрудники
- `GET /api/staff` — список сотрудников (ADMIN)
- `POST /api/staff` — добавить сотрудника (ADMIN)
- `DELETE /api/staff/:id` — удалить сотрудника (ADMIN)

### 5.7. QR-коды
- `GET /api/qr/generate?table_id={id}` — скачать QR (ADMIN)
- `GET /api/qr/preview?table_id={id}` — превью QR (ADMIN)

### 5.8. Загрузка файлов
- `POST /api/upload/image` — загрузить изображение (ADMIN)

## 6. Интеграции

### 6.1. WhatsApp
**Назначение:** Отправка уведомлений о заказах доставки.

**Настройка:**
1. В настройках ресторана указывается WhatsApp номер
2. В `.env` backend настраивается API (Green API или Twilio)

**Green API (рекомендуется):**
- Регистрация на https://green-api.com
- Получение `idInstance` и `apiTokenInstance`
- Добавление в `.env`:
  ```
  GREEN_API_ID=your_id
  GREEN_API_TOKEN=your_token
  ```

**Twilio (альтернатива):**
- Регистрация на https://twilio.com
- Получение `Account SID`, `Auth Token`, `WhatsApp From`
- Добавление в `.env`:
  ```
  TWILIO_ACCOUNT_SID=your_sid
  TWILIO_AUTH_TOKEN=your_token
  TWILIO_WHATSAPP_FROM=whatsapp:+1234567890
  ```

**Fallback:**
- Если API не настроено, показывается модальное окно с кнопкой "Открыть в WhatsApp" (wa.me ссылка)

## 7. Развертывание

### 7.1. Требования
- Node.js 18+
- MySQL 8.0+
- Docker (опционально, для MySQL)

### 7.2. Установка
1. Клонировать репозиторий
2. Настроить базу данных (см. `database/schema.sql`)
3. Настроить `.env` файлы (backend и frontend)
4. Установить зависимости: `npm install`
5. Запустить backend: `npm run dev`
6. Запустить frontend: `npm run dev`

### 7.3. Production build
- Backend: `npm run build && npm start`
- Frontend: `npm run build` (статические файлы)

## 8. Тестирование

### 8.1. Демо-аккаунты
- **SUPER_ADMIN:** superadmin@damdiqr.com / admin123
- **ADMIN:** admin@demo.com / admin123
- **STAFF:** staff@demo.com / staff123

### 8.2. Тестовые сценарии
1. Регистрация и вход
2. Создание ресторана (SUPER_ADMIN)
3. Создание меню (ADMIN)
4. Создание столиков и генерация QR (ADMIN)
5. Оформление заказа в ресторане (клиент)
6. Обработка заказа (STAFF)
7. Оформление заказа на доставку (клиент)
8. Получение уведомления в WhatsApp (ADMIN)

## 9. Будущие улучшения

### 9.1. Запланированные функции
- Платежная интеграция (Kaspi, Halyk Bank)
- Push-уведомления
- Многоязычность (казахский, русский, английский)
- Расширенная аналитика
- Экспорт отчетов (PDF, Excel)
- Интеграция с системами учета (1С, iiko)
- Мобильное приложение для сотрудников
- Система отзывов и рейтингов

### 9.2. Оптимизации
- Кэширование данных
- CDN для статических файлов
- WebSocket для real-time обновлений (вместо polling)
- Индексирование базы данных
- Миграция на TypeORM или Prisma

## 10. Поддержка и документация

### 10.1. Документация
- `README.md` — общая информация
- `ЗАПУСК.md` — инструкция по запуску
- `HOW_TO_SETUP_WHATSAPP.md` — настройка WhatsApp
- `TECHNICAL_SPECIFICATION.md` — данное ТЗ

### 10.2. Контакты
- Email поддержки: support@damdiqr.com
- WhatsApp: +7 777 123 4567

---

**Версия документа:** 1.0  
**Дата создания:** 2025-11-22  
**Последнее обновление:** 2025-11-22

